# NUVA v1.6 — Pre-Build Compile & APK Safety Audit

Date: 2026-08-26 · Branch `arena/01a038d4-nuva` · PR #15 (OPEN, not merged).
**No features, no architecture, no registry, no security changes.** One dead private function
removed (`pronounWithoutContext` — never called). Build hosts unreachable from this sandbox, so
per §9 this is STATIC validation only; **actual compilation must happen in GitHub Actions.**

## 1 · Build environment versions (as configured)

| Component | Version | Source |
|---|---|---|
| Gradle (workflow + wrapper properties) | 8.9 | pinned, self-contained download |
| AGP | 8.7.3 | libs.versions.toml |
| Kotlin | 2.0.20 | toml |
| Compose compiler | via `org.jetbrains.kotlin.plugin.compose` 2.0.20 (Kotlin-bundled) | toml — no version mismatch possible |
| KSP | 2.0.20-1.0.25 | toml |
| JDK (workflow) | temurin 17 | actions/setup-java@v5 |
| compileSdk / targetSdk / minSdk | 35 / 35 / 26 | app/build.gradle.kts |
| SDK installed by workflow | platform-tools, platforms;android-35, build-tools;35.0.0 | matches compileSdk |
| Room 2.6.1 · Retrofit 2.11.0 · OkHttp 4.12.0 · kotlinx-serialization 1.7.3 · Compose BOM 2024.09.03 · lifecycle 2.8.6 · activity-compose 1.9.2 · navigation 2.8.1 | mutually compatible set | toml ↔ build.gradle cross-checked |

## 2 · Compatibility result — PASS (static)

Gradle 8.9 ≥ AGP 8.7 requirement ✓ · KSP matches Kotlin ✓ · compose plugin ships with Kotlin 2.0 ✓ ·
serialization plugin matches ✓ · JDK 17 correct for AGP 8.x ✓ · jvmTarget 17 both sides ✓ ·
`testOptions.unitTests.isReturnDefaultValues = true` (JVM tests mock Android types) ✓ ·
repositories: google()+mavenCentral() with FAIL_ON_PROJECT_REPOS ✓ (all deps resolvable there).

## 3 · Dependency audit — PASS

Every `libs.*` reference in build files resolves to a toml alias (plugin aliases excluded by design).
No duplicates (BOM-managed Compose). One declared-but-unused lib found
(`androidx.compose.ui.tooling.preview`) — left untouched (no random dependency changes). No
unresolved imports found in source sweep.

## 4 · Source compilation audit (deterministic static sweep) — PASS

- Brace/paren balance: **all 60+ Kotlin files clean** (comment/string-aware checker).
- Exhaustiveness: `CommandExecutor.execute` when covers ALL 31 `NuvaAction` types; validator when
  covers all intents; ActionJson covers all actions (script-verified).
- R.string references ↔ `values/strings.xml`: **zero missing**; bn/en key sets identical.
- Nesting verified by re-read: `NuvaNotificationListener.Summary` nested correctly; companion
  functions intact; `WakeSessionState`/timeout blocks structurally sound; `VoiceController`
  withTimeout wraps collect with proper catch/finally.
- Deprecations (SmsManager.getDefault, getApplicationInfo, getParcelable) are warnings only —
  `allWarningsAsErrors` is NOT set.
- No `R.layout` references (Compose-only) ✓. Package names consistent (`com.nuva.assistant`).

## 5 · Manifest audit — PASS

application/MainActivity exported=true with LAUNCHER ✓ · NuvaAccessibilityService exported=false +
BIND_ACCESSIBILITY_SERVICE ✓ · NuvaNotificationListener exported=true + BIND_NOTIFICATION_LISTENER_SERVICE
(required) ✓ · FGS types: microphone for both mic services + FOREGROUND_SERVICE(+_MICROPHONE)
permissions (Android 14 requirement) ✓ · POST_NOTIFICATIONS runtime (Android 13) handled in
onboarding ✓ · no receivers (reboot = manual start by design) ✓ · no unnecessary permission added.
`<queries>` MAIN/LAUNCHER visibility ✓.

## 6 · Resource audit — PASS

strings: no duplicates, all names valid, bn/en parity ✓ · adaptive icons self-contained
(mipmap-anydpi-v26 → @color + @drawable both exist; minSdk 26 ⇒ anydpi-v26 covers every supported
device) ✓ · colors/themes/xml valid (parsed) ✓ · accessibility_service_config well-formed ✓.

## 7 · R8/ProGuard — N/A for the debug APK (minify off in debug); release rules already cover
kotlinx-serialization, Retrofit/OkHttp, Room, coroutines. No broad unsafe keeps added.

## 8 · Workflow audit — PASS with two honest facts

Live workflow (id 341039105, ACTIVE): checkout → JDK17 → SDK(platform 35/bt 35.0.0) → pinned
Gradle 8.9 → `:app:assembleDebug` → artifact upload. It does NOT use the wrapper and does NOT run
tests/lint/APK-existence — **and cannot be updated by this token** (workflow-file push rejected;
dispatch 403 — "Workflow dispatch unavailable because token lacks actions:write."). Per §13,
not bypassed. A complete wrapper-based replacement (tests → lint → assembleDebug → **explicit APK
existence gate** → upload, plus failure-report upload) is provided at
`docs/ci/apk-workflow-recommended.yml` for the maintainer to drop in.

Wrapper note: `gradle/wrapper/gradle-wrapper.jar` is NOT committed (policy); the recommended
workflow generates it deterministically (`gradle wrapper --gradle-version 8.9`) before using
`./gradlew` — or commit the jar once locally.

## 9 · Pre-build validation status

- Static: **DONE** (this document). Local Gradle run: **IMPOSSIBLE here** (services.gradle.org,
  dl.google.com, maven.google.com, repo.maven.apache.org all unreachable from the sandbox —
  connectivity test attached to the run log of this session). No build result is faked.
- Real compile/test/lint/APK: must come from the workflow. **Not claimed here.**

## 10 · Remaining external limitations

1. CI token: no `actions:write`/`workflows` → manual dispatch by maintainer (steps in the QA guide).
2. First CI run may still surface environment-level surprises; failure protocol §11 applies —
   fix the ROOT error only, no random changes.
3. `lint` can flag advisories (e.g., missing `@Preview` usage); only lint *errors* block —
   the recommended workflow uses `lintDebug` and reports are uploaded on failure.

**Acceptance (§12): NOT yet claimable — no physical `app-debug.apk` exists until the workflow
produces it.**
